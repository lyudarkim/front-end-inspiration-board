import React from 'react';
import './BoardList.css';

const BoardList = () => {
    return (
        <section>
            This is the Board List
        </section>
    );
};

export default BoardList;